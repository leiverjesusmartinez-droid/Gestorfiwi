name: gestorfiwi
description: Un gestor Wi-Fi para Android compatible con Samsung Galaxy A13 5G.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  wifi_iot: ^0.3.19

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
